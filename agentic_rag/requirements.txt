# Required Python Version: >=3.11, <3.12 (Tested with Python 3.11.0)

# Core orchestration & state machine
langgraph==1.2.2
langchain==1.3.2
langgraph-checkpoint-sqlite
langgraph-checkpoint-postgres
langchain-openai==1.2.2          # For OpenAI models & embeddings
langchain-community==0.4.2 
langchain-cohere==0.1.5      # Community loaders (web, PDF, etc.)

# Vector stores & retrieval
faiss-cpu==1.14.2                 # FAISS for similarity search
chromadb==1.5.9                  # Optional: Chroma as vector DB
sentence-transformers==5.5.1      # Cross-encoder reranking & embedding models

# Web search & external tools
tavily-python==0.7.25             # Tavily API for fallback web search

# API & server
fastapi==0.136.3
uvicorn[standard]==0.48.0        # Web server (includes standard dependencies like httptools)

# Configuration & environment
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.1

# Token counting & text processing
tiktoken==0.13.0

# Document loading (ingestion pipeline)
pypdf==6.12.2                     # PDF loading
beautifulsoup4==4.14.3            # Web loading
requests==2.34.2                  # HTTP calls

# State persistence (checkpointers)
redis==8.0.0                      # For RedisSaver (production)
# SQLite is built-in – no extra package needed for SqliteSaver

# Testing & development
pytest==9.0.3

# Optional but useful
numpy==2.4.6                      # Often used by embeddings
pandas==3.0.3                     # If you handle tabular data